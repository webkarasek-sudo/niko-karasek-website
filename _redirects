# Хост-редиректы (apex ↔ www) НЕ прописываем вручную —
# Netlify сам перенаправляет на основной домен (Domain management → Primary domain).
# Ручные правила здесь конфликтуют с этим и вызывают петлю редиректов.
#
# Пример SPA-фолбэка (не нужен для многостраничного сайта):
# /*   /index.html   200
